import 'package:flutter/material.dart';

class TownMenu extends StatelessWidget {
  const TownMenu({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
